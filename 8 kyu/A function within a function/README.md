# :star:Removing Elements

Given an input n, write a function always that returns a function which returns n. Ruby should return a lambda or a proc.

# :star:Example:

> var three = always(3);<br>
  three(); // returns 3